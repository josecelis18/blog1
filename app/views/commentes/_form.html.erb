<%= form_with(model: commente) do |form| %>
  <% if commente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commente.errors.count, "error") %> prohibited this commente from being saved:</h2>

      <ul>
        <% commente.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post %>
    <%= form.text_field :post %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
